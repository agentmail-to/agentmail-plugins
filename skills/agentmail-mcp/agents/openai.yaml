interface:
  display_name: "AgentMail MCP Setup"
  short_description: "Configure the AgentMail MCP server"
  default_prompt: "Use $agentmail-mcp to configure or troubleshoot the AgentMail MCP connection."
policy:
  allow_implicit_invocation: true
